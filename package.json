{
  "version": "1.3.2",
  "description": "Hyperbrowser Model Context Protocol Server",
  "name": "hyperbrowserai-mcp-server",
  "main": "./dist/server.js",
  "bin": {
    "hyperbrowser-mcp": "dist/server.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "node -e \"const fs=require('fs');if(fs.existsSync('dist'))fs.rmSync('dist',{recursive:true})\" && tsc && node -e \"try{require('fs').chmodSync('dist/server.js','755')}catch(e){}\"",
    "start": "node dist/server.js",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "format": "prettier --write .",
    "build-resources": "node --loader ts-node/esm scripts/build_resources.ts && cp summarized.json src/resources/static/data/summarized.json && rm summarized.json && rm static_data.json",
    "prepare": "npm run build",
    "publish": "node -e \"const fs=require('fs');['dist','node_modules'].forEach(d=>{if(fs.existsSync(d))fs.rmSync(d,{recursive:true})})\" && npm i && npm run build && npm publish"
  },
  "dependencies": {
    "@hyperbrowser/sdk": "^0.48.1",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ajv": "^8.17.1",
    "axios": "^1.8.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^16.11.7",
    "openai": "^4.87.3",
    "ts-node": "^10.9.2",
    "typescript": "^4.5.2"
  },
  "keywords": [
    "mcp",
    "hyperbrowser",
    "web-scraping",
    "crawler",
    "content-extraction"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hyperbrowserai/mcp.git"
  },
  "types": "./dist/server.d.ts",
  "bugs": {
    "url": "https://github.com/hyperbrowserai/mcp/issues"
  },
  "homepage": "https://github.com/hyperbrowserai/mcp#readme",
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "ISC"
}
